# MVS P2P server
Rust based.

Still wip but this a p2p server that runs along side mvs and with an [http server ](https://github.com/multiversuskoth/mvs-http-server/tree/main/src) you can have the game connect to it.
This will then act as a server from mvs side but it allows p2p communication
